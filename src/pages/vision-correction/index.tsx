import Page from '@/components/Page';

/**
 * Home/Landing page component for the App
 *
 * @export
 * @returns {JSX.Element}
 */
export default function index(): JSX.Element {
    return (
        <Page title="Vision Correction">
            <h1>Vision Correction page</h1>
        </Page>
    );
}
